{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach to NestJS Debug",
            "port": 9229, // Default port for --debug
            "restart": true,
            "sourceMaps": true,
            "timeout": 5000,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": null
        }
    ]
}
